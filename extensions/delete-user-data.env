# Configuration for Delete User Data extension (GDPR Compliance)

# Firestore paths containing user data to delete (comma-separated)
# Use {UID} placeholder for user ID
# Example: users/{UID},orders/{UID},posts/{UID}
FIRESTORE_PATHS=users/{UID},transactions/{UID},messages/{UID},conversations/{UID},notifications/{UID},reviews/{UID}

# Cloud Storage paths containing user data to delete (comma-separated)
# Use {UID} placeholder for user ID
# Example: users/{UID},profiles/{UID}
STORAGE_PATHS=users/{UID},products/{UID},verification/{UID}

# Realtime Database paths to delete (comma-separated, optional)
# RTDB_PATHS=users/{UID}

# Enable automated deletion when user is deleted (true/false)
# ENABLE_AUTO_DELETE=true
